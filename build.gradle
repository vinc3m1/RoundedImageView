buildscript {
  repositories {
    mavenCentral()
  }

  dependencies {
    classpath 'com.android.tools.build:gradle:1.2.3'
  }
}

allprojects {
  group 'com.makeramen'
  version '2.1.1'
  repositories {
    mavenCentral()
  }
}

ext {
  compileSdkVersion = 22
  buildToolsVersion = "22.0.1"
}