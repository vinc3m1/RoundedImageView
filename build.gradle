buildscript {
  repositories {
    mavenCentral()
  }

  dependencies {
    classpath 'com.android.tools.build:gradle:1.3.1'
  }
}

allprojects {
  group 'com.makeramen'
  version '2.2.0'
  repositories {
    mavenCentral()
  }
}

ext {
  compileSdkVersion = 23
  buildToolsVersion = "23.0.1"
}