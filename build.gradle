buildscript {
  repositories {
    mavenCentral()
  }

  dependencies {
    classpath 'com.android.tools.build:gradle:1.1.0'
  }
}

allprojects {
  group 'com.makeramen'
  version '2.0.0'
  repositories {
    mavenCentral()
  }
}

ext {
  compileSdkVersion = 21
  buildToolsVersion = "21.1.2"
}